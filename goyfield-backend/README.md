## Build and start

This project requires Prisma client.
To generate it use `npm run prisma:generate:v2`.

To start project without building use `npm run dev`.

To build project use `npm run build`.

You can delete compiled code using `npm run clean` or build ignoring build cache using `npm run rebuild`.

To start built project use `npm run start`.